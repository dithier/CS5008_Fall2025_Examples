
#include <stdio.h>
#include <string.h>

int main() {
    // initialize char array without size specified

    // print each character in string

    // create string by initializing char array without \0

    // print each character in string (is there garbage?)

    return 0;
}
