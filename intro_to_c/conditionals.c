#include <stdio.h>
#include <stdlib.h>

int main(int arg, char* argv[]) {
    // give usage which takes one int arg

    // read in number and display to user

    // if statement

    // if else

    // if, else if, else

    return 0;
}
